using DataFrames

function firstUncertainty()
    schedule = readtable("Data/processed/scheduled.csv", header=true, makefactors=true)
    #get the last week's schedule
end

function worstCase(x,A)
    # solve worst case min problem - formulate WC
end
